import {makeStyles} from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    root: {
        marginTop: '20px',
        width: '600px',
        height: '600px',
        flexGrow: 1,

    },
    button: {
        height: '48px'
    }
}));